name: Build and Deploy

on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/my-custom-wordpress:latest

  deploy-to-k8s:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Kubeconfig
        env:
          KUBE_DATA: ${{ secrets.KUBECONFIG }}
        run: |
          mkdir -p ~/.kube
          # We use the variable directly to preserve the structure
          echo "$KUBE_DATA" | base64 -d > ~/.kube/config
          chmod 600 ~/.kube/config
      - name: Apply Kubernetes Manifests
        run: |
          kubectl apply -f mysql-pvc.yaml
          kubectl apply -f mysql-dep.yaml
          kubectl rollout restart deployment/my-web-wordpress
